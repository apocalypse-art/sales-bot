# ─── Twitter / X API Credentials ─────────────────────────────────────────────
# Get these from: https://developer.twitter.com/en/portal/dashboard
# Create a project → App → Keys and Tokens
# Make sure the app has READ and WRITE permissions

TWITTER_APP_KEY=your_api_key_here
TWITTER_APP_SECRET=your_api_key_secret_here
TWITTER_ACCESS_TOKEN=your_access_token_here
TWITTER_ACCESS_SECRET=your_access_token_secret_here


# ─── Reservoir Webhook Secret ────────────────────────────────────────────────
# Optional but recommended: a secret string you set in the Reservoir dashboard
# to verify that incoming webhooks are really from Reservoir.
# Can be any random string, e.g.: openssl rand -hex 32

RESERVOIR_WEBHOOK_SECRET=your_reservoir_webhook_secret_here


# ─── Server Port ─────────────────────────────────────────────────────────────
# Railway sets this automatically; you only need this for local testing.

PORT=3000
